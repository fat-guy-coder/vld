// ==================================================================================================
// Parser
// ==================================================================================================

/**
 * @description 将模板字符串解析为抽象语法树 (AST)。
 * @param template - 要解析的模板字符串。
 * @returns 一个代表模板结构的 AST 对象。
 * @internal
 */
export function parse(template: string) {
  // TODO: 实现模板解析逻辑
  // 1. 创建解析上下文
  // 2. 逐字符解析模板
  // 3. 处理标签、属性、文本和插值
  // 4. 构建 AST 节点

  // 临时返回一个根节点
  return {
    type: 'Root',
    children: [],
  };
}

